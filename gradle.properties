# OkHttp's Android artifact depends on androidx.annotation and androidx.startup, so the
# AndroidX namespace is on; our own app sources still use framework APIs only (TM-005).
android.useAndroidX=true
org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
kotlin.code.style=official
