# No AndroidX today (core uses okhttp-jvm; app sources are framework-only, TM-005). The flag
# stays on so a future androidx dependency does not trip the AAR metadata check.
android.useAndroidX=true
org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
kotlin.code.style=official
